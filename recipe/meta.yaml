{% set version = "1.22.6" %}

package:
  name: geocoder
  version: {{ version }}

source:
  fn: geocoder-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/g/geocoder/geocoder-{{ version }}.tar.gz
  sha256: 12be1fb511f1c407ac956415aa274fac820b3888f6aea1de9ab5124b33965b06
  patches:
    - setup_py-utf8.patch  # [win and py3k]

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt
  preserve_egg_dir: True
  entry_points:
    - geocode=geocoder.cli:cli

requirements:
  build:
    - python
    - setuptools
  run:
    - python
    - requests
    - ratelim
    - click
    - six

test:
  imports:
    - geocoder

about:
  home: https://github.com/DenisCarriere/geocoder
  license: Apache 2.0
  license_file: LICENSE
  summary: 'Geocoder is a simple and consistent geocoding library.'

extra:
  recipe-maintainers:
    - ocefpaf
